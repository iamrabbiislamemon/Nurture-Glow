You are working inside an **existing** React + TypeScript + Vite app in this repo, and the Hospitals screen is **already implemented**.

**Project reality (current status you must respect)**
- React is **v19** (not 18). Router is **HashRouter** and routes live in `Nurture-Glow/frontend/components/Layout.tsx`.
- The Hospitals route already exists: `/hospitals` renders the current directory UI in `Nurture-Glow/frontend/pages/Hospitals.tsx`.
- That page already has:
  - Hospital directory data from backend via `db.getHospitals()` in `Nurture-Glow/frontend/services/db.ts`
  - Geolocation + live tracking using `navigator.geolocation.watchPosition`
  - Client-side Haversine distance sorting (“nearest” sort)
  - A map area using an OpenStreetMap iframe, plus Google Maps “open” links
- Tailwind utility classes are available via **CDN** in `Nurture-Glow/frontend/index.html` (not a Tailwind build pipeline).

Your job is **NOT** to rebuild Hospitals from scratch. Your job is to **add Google Maps + Places “Nearest Hospitals (Near Me)” as an additional feature** while keeping the existing Hospitals UI intact.

---

## FEATURE TO ADD: “Nearest Hospitals (Google Places)” inside the existing Hospitals page

### GOAL
Enhance the existing Hospitals experience by adding a new “Near Me (Google)” mode/section that:
1) Requests the user’s current location on button click (support refresh).
2) Renders a real Google Map centered on the user.
3) Uses **Google Places Nearby Search** (`type: "hospital"`) to find nearby hospitals and shows them:
   - As markers on the map
   - As a sortable list (distance ascending)
4) Clicking a hospital in the list focuses the map/marker and opens an InfoWindow.
5) Each hospital card shows: name, vicinity/address, rating, open_now (if provided), distance (km), and a “Directions” link (Google Maps directions from user → hospital).
6) Handles errors gracefully (permission denied, timeout, no results, API not loaded, missing key).

### UX INTEGRATION (important)
- Do **not** remove or break the existing directory UI in `Nurture-Glow/frontend/pages/Hospitals.tsx`.
- Implement the Google Places feature as **one of these** (pick the simplest that fits):
  - Option A (preferred): an **in-page tab toggle** inside the Hospitals screen: “Directory” (existing) vs “Near Me (Google)”.
  - Option B: an in-page expandable section/button that opens the “Near Me (Google)” UI under/over the existing layout.
- Do **not** add a new route unless absolutely necessary. If you do add one, provide a route snippet for `Nurture-Glow/frontend/components/Layout.tsx` but keep changes minimal.

---

## STACK / CONSTRAINTS
- React 19 + TypeScript + Vite
- Use `@react-google-maps/api` (install if missing)
- No mock data: real Places results only
- Compute distance client-side using Haversine (no paid Distance Matrix)
- Radius defaults to 5000m, user can change radius via dropdown: 1km/3km/5km/10km
- Do NOT expose API key in code. Read from Vite env: `import.meta.env.VITE_GOOGLE_MAPS_API_KEY`
- Prefer small, modular files consistent with this repo’s structure (note: this repo does not use a `/src` folder for app code)

---

## FILE/FOLDER STRUCTURE TO USE (match this project)
Create these new files (new folders are OK):
- `Nurture-Glow/frontend/services/googleMaps.ts`
- `Nurture-Glow/frontend/hooks/useGeolocation.ts`
- `Nurture-Glow/frontend/hooks/useNearbyHospitals.ts`
- `Nurture-Glow/frontend/components/hospitals/HospitalFinder.tsx`
- `Nurture-Glow/frontend/components/hospitals/HospitalList.tsx`
- `Nurture-Glow/frontend/components/hospitals/HospitalMap.tsx`

Then do a **minimal edit** to integrate it into:
- `Nurture-Glow/frontend/pages/Hospitals.tsx`

Optional (only if you decide to make it route-ready instead of tabbed):
- `Nurture-Glow/frontend/pages/HospitalsNearMePage.tsx`

---

## IMPLEMENTATION DETAILS

### 1) Environment / Keys
- Add an example snippet to `Nurture-Glow/frontend/.env.example`:
  - `VITE_GOOGLE_MAPS_API_KEY=YOUR_KEY`
- For local dev, instruct user to create **`Nurture-Glow/frontend/.env.local`** (not committed) with:
  - `VITE_GOOGLE_MAPS_API_KEY=...`

### 2) Dependencies
- If `@react-google-maps/api` is not installed, install it.
- If TypeScript can’t find `google.maps` types, install `@types/google.maps` (only if needed by the build).

### 3) Google Maps Loader Helper
In `Nurture-Glow/frontend/services/googleMaps.ts`:
- Create a small typed helper for `useJsApiLoader` config:
  - `googleMapsApiKey: import.meta.env.VITE_GOOGLE_MAPS_API_KEY`
  - `libraries: ['places']`
- Expose a function or constant returning the loader options.

### 4) Geolocation Hook
In `Nurture-Glow/frontend/hooks/useGeolocation.ts` return:
- `{ location, accuracy, loading, error, getLocation, watchLocation, stopWatching }`
- Use sensible options (`enableHighAccuracy`, `timeout`, `maximumAge`)
- This should be reusable and should cover permission/timeouts with user-friendly error strings.

### 5) Nearby Hospitals Hook (Google Places)
In `Nurture-Glow/frontend/hooks/useNearbyHospitals.ts`:
- Input: `{ location, radius }`
- Output:
  - `{ hospitals, selectedHospital, loading, error, searchHospitals, selectHospital, clearSelection }`
- Use `google.maps.places.PlacesService.nearbySearch`
- Map results into a typed model:

```ts
export type HospitalPlace = {
  placeId: string;
  name: string;
  location: { lat: number; lng: number };
  vicinity?: string;
  rating?: number;
  userRatingsTotal?: number;
  openNow?: boolean;
  types?: string[];
  distanceKm: number;
};
```

- Sort hospitals by `distanceKm` ascending.
- Optional enhancement: only when selected, call `PlacesService.getDetails({ placeId, fields: [...] })` to get `formatted_address`, `website`, `formatted_phone_number` (don’t overcomplicate).

### 6) UI Components
In `Nurture-Glow/frontend/components/hospitals/HospitalFinder.tsx`:
- Controls row:
  - “Use My Location” (requests GPS)
  - “Refresh” (rerun nearby search)
  - radius dropdown (1/3/5/10 km)
  - status text (loading / errors)
- Layout:
  - Desktop: split map/list (60/40)
  - Mobile: stacked
- Must feel consistent with existing Hospitals page styling (Tailwind utility classes are fine).

In `Nurture-Glow/frontend/components/hospitals/HospitalMap.tsx`:
- Use `GoogleMap`, `Marker`, `InfoWindow`
- Show distinct user marker
- Show hospital markers
- Marker click selects hospital
- InfoWindow shows: name, rating, distance, directions link

In `Nurture-Glow/frontend/components/hospitals/HospitalList.tsx`:
- Clickable list items, highlight selected
- Show open/closed badge if `openNow` is defined

### 7) Integration into existing Hospitals page
In `Nurture-Glow/frontend/pages/Hospitals.tsx`:
- Add a simple toggle/tab/button that switches between:
  - existing directory UI (unchanged)
  - new `HospitalFinder` (Google Near Me UI)
- Keep all existing features (favorites, filters, OSM iframe, etc.) working.

### 8) Edge cases
- If location not available: show CTA to request permission
- If Google API fails to load: message about key, billing, or restrictions
- If no results: “No hospitals found within X km; try increasing radius.”

---

## OUTPUT REQUIREMENTS (adapted for this repo)
- Start by listing the **new files** you will create.
- For **new files**, output full contents.
- For the existing `Nurture-Glow/frontend/pages/Hospitals.tsx`, do **not** rewrite the entire file (it’s large). Provide a **minimal patch-style change** (show only the exact added/changed blocks and where they go).
- Include a short “How to test” checklist at the end.

Now implement it.
